apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - shopease-frontend-dev-sealedsecret.yaml

namespace: shopease-dev

patches:
  - path: deployment-patch.yaml
  - path: service-patch.yaml
